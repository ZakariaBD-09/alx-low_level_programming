#include "main.h"
/**
 * add - a founction that adds two integers and return the result.
 * @a: the first integer in the function parameter.
 * @b: the second integer in the function parameter.
 *
 * Return: the intgers a and b.
 *
 */
int add(int a, int b)
{
	return (a + b);
	_putchar('\n');
}
